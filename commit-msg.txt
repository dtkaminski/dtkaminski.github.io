feat(marketing): wire self-serve signup into the primary funnel CTAs

The 5 marketing "Apply for the founding cohort" CTAs all pointed at the gated
application page (auth/apply.html). The self-serve create-account flow
(auth/signup.html — email + brand name + auto-derived editable slug, posts to
the deployed signup edge fn, "check your email" success, claim-aware) already
existed but was only linked from secondary/internal spots.

Repoint the four primary CTAs (nav, hero, self-serve pricing card, final CTA)
to auth/signup.html with create-account wording ("Get started" / "Create your
account"), dropping the £750 / 10-spots / 48hr-review framing on those CTAs and
the final-CTA headline + sub. The Managed price-card CTA still points at
apply.html, reworded "Apply for Managed (done-for-you) →" — apply.html is
preserved intact as the done-for-you application. No backend change (signup is
already deployed + verified); no duplicated signup page.

Note: surrounding narrative (hero eyebrow/sub, pricing section headline,
<title>) still carries cohort/£750 framing — left for a separate positioning
pass rather than over-editing marketing copy here.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
